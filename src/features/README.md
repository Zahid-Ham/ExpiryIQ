# Features Directory

Houses module-scoped capabilities (e.g., authentication, record-management, expiry-tracker). Each feature subdirectory contains its own components, hooks, services, types, and logic to keep the project highly modular and scalable.
